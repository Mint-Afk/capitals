#!/usr/bin/python3.1

''' import modules used here -- sys is a very standard one, 
    useful to accept inputs'''

#print("The name of this program is {}".format(sys.argv[0]))
#print("You entered {}".format(to_print))

from package import capitals
import sys

if len(sys.argv) > 1:
    input = sys.argv[1]
    state,capital = capitals.check_capital(str(input))
    print("The capital of {} is {}".format(state, capital))

else:
    print("Tell what capital you wanna know bout!")
    exit()

 
